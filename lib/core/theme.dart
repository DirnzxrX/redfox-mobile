import 'package:flutter/material.dart';

class AppColors {
  // --- Warna Utama (Dark Theme) ---
  static const Color background = Color(0xFF0B1221); // Biru sangat gelap (background utama)
  static const Color cardBg = Color(0xFF1A233A); // Biru gelap keabu-abuan (untuk kartu/container)
  
  // --- Warna Teks ---
  static const Color textMain = Color(0xFFFFFFFF); // Teks utama (putih terang)
  static const Color textSecondary = Color(0xFF94A3B8); // Teks sekunder (abu-abu kebiruan)
  
  // --- Warna Aksen (Neon) ---
  static const Color primaryAccent = Color(0xFF00D1FF); // Cyan nyala (Garis pinggir & highlight utama)
  
  // --- Warna Status & Sentimen ---
  static const Color positive = Color(0xFF10B981); // Hijau neon (Positive Mentions)
  static const Color negative = Color(0xFFEF4444); // Merah neon (Negative Mentions & Alerts)
  static const Color neutral = Color(0xFF64748B);  // Abu-abu terang (Neutral Mentions)
  static const Color warning = Color(0xFFF59E0B);  // Oranye neon (Threat Score)

  // --- Warna Sumber / Platform (Disesuaikan untuk Dark Theme) ---
  static const Color youtube = Color(0xFFEF4444); // Merah
  static const Color instagram = Color(0xFFD946EF); // Pink/Ungu
  static const Color xTwitter = Color(0xFF38BDF8); // Biru muda (X/Twitter)
  static const Color news = Color(0xFF0EA5E9); // Biru terang
  static const Color tiktok = Color(0xFFE2E8F0); // Putih keabu-abuan
  static const Color facebook = Color(0xFF3B82F6); // Biru khas FB
  static const Color telegram = Color(0xFF0088CC); // Biru khas Telegram
}